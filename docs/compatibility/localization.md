# 🇨🇳 多语言自动本地化

otex 提供了一套“感知型”的本地化框架，能够根据你指定的 `english` 选项自动调整文档的所有界面语言。

---

## 1. 核心开关：`english` 选项

在引入 otex 时指定：

```latex
\usepackage[english=true]{otex}  % 界面保持全英文
\usepackage[english=false]{otex} % 界面开启中文支持 (默认)
```

---

## 2. 定理环境的自动翻译

otex 内置了 11 种常用的定理环境，它们会根据语言选项自动切换名称：

| 环境名 | `english=false` (默认) | `english=true` |
| :--- | :--- | :--- |
| `theorem` | 定理 | Theorem |
| `lemma` | 引理 | Lemma |
| `proposition` | 命题 | Proposition |
| `corollary` | 推论 | Corollary |
| `definition` | 定义 | Definition |
| `example` | 例 | Example |
| `remark` | 注 | Remark |
| `proof` (环境) | 证明 (自动补齐后缀项目) | Proof |

---

## 3. CTEX 深度集成

当 `english=false` 时，otex 会利用 `ctex` 宏包的强大功能，但做了如下优化以增强兼容性：

- **智能 Heading 避让**：如果检测到你正在使用已经带有页眉配置的复杂模板（如 `hithesis`），otex 会自动以 `heading=false` 模式加载 `ctex`，以防破坏模板原有的排版样式。
- **平滑切换**：即使在非 CTEX 文档类（如 `article`）中，也能获得完美的中文排版效果。

---

## 4. 引用标注与引文列表 (cleveref)

我们为 `cleveref` 宏包提供了深度的中文本地化支持。当你使用 `\cref{fig:xxx}` 时：

- **中文模式下**：自动显示为 “图 1-1”。
- **英文模式下**：自动显示为 “Figure 1-1”。

目前支持的本地化标签包括：`figure`, `table`, `equation`, `theorem`, `lemma`。

---

## 5. 算法环境

无论你选择 `algorithm2e` 还是 `algorithmicx` 作为后端，otex 都会尝试将“Algorithm”字样翻译为“算法”，并调整相应的关键词（如 Input -> 输入, Output -> 输出）。

---
[返回兼容性首页](README.md) | [返回文档首页](../README.md)
