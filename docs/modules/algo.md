# 🤖 算法环境模块 (`algo`)

`algo` 模块为展示伪代码和算法流程提供了统一接口。

---

## 1. 加载的宏集

本模块支持两种主流的算法排版包，通过 `algo-backend` 切换：

- **`algorithm2e` (默认)**：功能极强，支持多种 ruled 样式和行号。
- **`algorithmicx` (`algpseudocode`)**：语法更接近编程语言，易于定制。

## 2. 配置选项

- `algo`: (Boolean) 是否加载此模块，默认为 `true`。
- `algo-backend`: 可选值为 `algorithm2e` (默认) 或 `algorithmicx`。

## 3. 使用示例 (`algorithm2e`)

```latex
\begin{algorithm}[H]
    \caption{求和算法}
    \Input{$a, b$}
    \Output{$sum = a + b$}
    \If{$a > 0$}{
        $sum \leftarrow a + b$
    }
\end{algorithm}
```

---
[返回模块总览](README.md)
