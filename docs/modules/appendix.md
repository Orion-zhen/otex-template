# 📎 附录模块 (`appendix`)

`appendix` 模块为文档的附录部分提供增强支持。

---

## 1. 加载的宏包

- `appendix`: 用于格式化附录标题和目录显示。

## 2. 使用示例

```latex
\begin{appendices}
    \section{源代码}
    这里是附录内容...
\end{appendices}
```

## 3. 配置选项

- `appendix`: (Boolean) 是否加载此模块，默认为 `false` (需要手动开启)。

---
[返回模块总览](README.md)
